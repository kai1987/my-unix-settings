my-unix-settings
================

## Install

```bash
cd ~ && \
mkdir -p tmp && \
cd temp && \
git clone git@github.com:yi/my-unix-settings.git && \
mv ./my-unix-settings/.git ~ && \
cd ~ && \
git reset --hard HEAD && \
rm -rf ~/temp/my-unix-settings
```
